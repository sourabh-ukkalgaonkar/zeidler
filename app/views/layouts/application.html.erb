<!DOCTYPE html>
<html>
  <head>
    <title>Zeidler</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-sm bg-light">
      <!-- Links -->
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if current_user.present? %>
            <%= link_to 'Logout', '/logout', method: :delete %>
          <% else %>
            <% unless is_session_or_registration?(params[:controller]) %>
              <%= link_to 'Login |', '/login'  %>
              <%= link_to 'Sing Up', '/sign-up' %>
            <% end %>
          <% end %>
        </li>
      </ul>
    </nav>
    <%= yield %>
  </body>
</html>
